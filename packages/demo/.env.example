# Optional — enables the AI docs assistant (/api/ai). Without it the panel
# honestly reports that the assistant is not configured.
GROQ_API_KEY=

# Optional — Groq model id. Defaults to llama-3.3-70b-versatile.
GROQ_MODEL=
