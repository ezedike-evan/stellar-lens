# ─────────────────────────────────────────────────────────────────────────────
# Purpose:  Runs GitHub CodeQL static analysis on TypeScript source files to
#           detect security vulnerabilities in the codebase itself.
# Triggers: push to main (source files only), pull_request targeting main
#           (source files only), weekly schedule on Sunday at 02:00 UTC
# Paths:    packages/sdk/src/**/*.ts (push/PR only — schedule ignores paths)
# ─────────────────────────────────────────────────────────────────────────────

name: CodeQL

on:
  push:
    branches:
      - main
    paths:
      - 'packages/sdk/src/**/*.ts'
  pull_request:
    branches:
      - main
    paths:
      - 'packages/sdk/src/**/*.ts'
  schedule:
    - cron: '0 2 * * 0'

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@65c74964a9ed8c44ed2f7d6f9f5491c4bd3c28d4 # v3.28.x
        with:
          languages: javascript-typescript
          build-mode: none
          queries: security-and-quality

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@65c74964a9ed8c44ed2f7d6f9f5491c4bd3c28d4 # v3.28.x
        with:
          category: '/language:javascript-typescript'
